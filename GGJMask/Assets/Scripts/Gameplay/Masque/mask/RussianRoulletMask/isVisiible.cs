using UnityEngine;

namespace Masque.mask.RussianRoulletMask
{
    public class IsVisiible : MonoBehaviour
    {
        /*[SerializeField] private RussianRoulletEffect roulletEffect;
        private int dead;
        [SerializeField] private GameObject ui;

        public void OnEnable()
        {
            roulletEffect.IsVisibile+=RoulleteTime;
        }

        public void OnDisable()
        {
            roulletEffect.IsVisibile-=RoulleteTime;
        }

        private void RoulleteTime(RussianRoulletEffect obj)
        {
            ui.SetActive(true);
        }

        public void Result()
        {
            dead = Random.Range(1, 100);

            if (dead < 15)
            {
                Debug.Log(" is dead");
                Debug.Log(dead);
                ui.SetActive(false);
            
            }
            else
            {
                Debug.Log(" not dead");
                Debug.Log(dead);
                ui.SetActive(false);
            }
        }*/
    }
}
